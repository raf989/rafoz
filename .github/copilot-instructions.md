# 🤖 Copilot Instructions for Rafoz Matrix Manager

## Архитектура и ключевые файлы
- [src/App.tsx](src/App.tsx) — главный контейнер и оркестратор UI/стейта; здесь живут модальные окна, фильтрация, импорт/экспорт и навигация по папкам/таблицам.
- [src/types.ts](src/types.ts) — базовая модель данных: `Folder` → `ProjectTable` → `DynamicRow`, плюс `Column`, `ActiveFilter`, `TableType`. Новые данные всегда формируются по этим типам.
- [src/constants.ts](src/constants.ts) — все лимиты и дефолты (имена, описания, комментарии, лимит отображения ячейки, пагинация). Не хардкодить числа вне этого файла.
- [src/utils](src/utils) — утилиты для форматов и валидации: `truncate()` и лимит таблицы, `parseCSV()/stringifyCSV()`, `validateSegments()`.
- [src/hooks](src/hooks) — выделенные хелперы логики (статистика колонок, CRUD для папок/таблиц), но реальный рендер и сценарии всё ещё держатся в `App.tsx`.

## Потоки данных и хранение
- Основное состояние хранится в `localStorage` под ключом `rafoz_data` (см. [src/App.tsx](src/App.tsx)). Сайдбар — `rafoz_sidebar_collapsed`.
- Фильтрация: `ActiveFilter` поддерживает `include/exclude`, формируется по уникальным значениям колонок (см. [src/hooks/useColumnStats.ts](src/hooks/useColumnStats.ts)).
- CSV: парсер/стрингер в [src/utils/csv.ts](src/utils/csv.ts); импорт добавляет колонки/строки на основе заголовков.

## Проектные правила UI
- Лимиты ввода строго соблюдаются (имя/описание/комментарий/ячейка); при превышении ввод блокируется, счетчик краснеет (см. [src/App.tsx](src/App.tsx) и [src/constants.ts](src/constants.ts)).
- Табличные значения визуально обрезаются до `TABLE_CELL_LIMIT` через `truncate()`; полный текст показывается в Record View (см. [src/utils/formatting.ts](src/utils/formatting.ts)).
- Pairwise режим: ввод с `/` генерирует все комбинации сегментов (см. [src/App.tsx](src/App.tsx) и правила в [README.md](README.md)).
- Для статусов/бейджей использовать [src/components/Badge.tsx](src/components/Badge.tsx).

## Workflow
- Установка: `npm install`
- Dev: `npm run dev`
- Build: `npm run build` (сначала `tsc`)
- Preview: `npm run preview`
- Тестов нет; стандартные Vite/TypeScript скрипты (см. [package.json](package.json)).

## Интеграции и окружение
- React 19 + Vite + TailwindCSS; Tailwind подключён через [index.html](index.html).

Если что-то непонятно или не отражено (например, конкретная модалка/поток), скажи — уточню и допишу.
