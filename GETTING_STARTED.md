# 🎓 НОВИЧОК? ЧИТАЙ СЮДА!

## 👋 Добро пожаловать в Rafoz Matrix Manager!

Проект был полностью переструктурирован для удобства разработки. Вот план для быстрого старта:

---

## 📚 Шаг 1: Понимание структуры (5 минут)

Открой этот файл и прочитай:
```
PROJECT_STRUCTURE.md
```

Это поможет тебе понять где какие файлы находятся.

---

## 🔍 Шаг 2: Быстрая справка (5 минут)

Когда нужно быстро найти функцию:
```
QUICK_REFERENCE.md
```

Здесь собраны все функции, hooks и компоненты с примерами.

---

## 📖 Шаг 3: Детальное руководство (15 минут)

Когда хочешь всё узнать в деталях:
```
DEVELOPER_GUIDE.md
```

Тут есть примеры кода, как добавлять новые функции и многое другое.

---

## 🗺️ Шаг 4: Исследование кода (30 минут)

Теперь откройте папку `src/` и посмотрите:

### 1️⃣ `src/components/`
```typescript
// Здесь UI компоненты
import { Badge } from './src/components';
<Badge variant="primary">Hello</Badge>
```

### 2️⃣ `src/hooks/`
```typescript
// Здесь бизнес-логика с состоянием
const { folders, createFolder } = useFolders();
```

### 3️⃣ `src/utils/`
```typescript
// Здесь чистые функции без состояния
const formatted = formatDate(Date.now());
```

### 4️⃣ `src/types.ts`
```typescript
// Здесь TypeScript интерфейсы
interface ProjectTable { ... }
```

### 5️⃣ `src/constants.ts`
```typescript
// Здесь константы
const NAME_LIMIT = 50;
```

---

## 🚀 Шаг 5: Запуск приложения

```bash
# Установи зависимости (если ещё не установлены)
npm install

# Запусти dev-сервер
npm run dev

# Открой браузер
http://localhost:3002
```

---

## 💡 Первая задача: Добавить функцию

### Задача: Добавить функцию для разворота текста

**1. Открой `src/utils/formatting.ts`**

**2. Добавь функцию:**
```typescript
export const reverse = (str: string) => {
  return str.split('').reverse().join('');
};
```

**3. Используй где-нибудь в компоненте:**
```typescript
import { reverse } from './src/utils';

const backwards = reverse('Hello');  // "olleH"
```

**Готово! 🎉**

---

## 🎯 Вторая задача: Создать компонент

### Задача: Создать компонент Button

**1. Создай файл `src/components/Button.tsx`:**
```typescript
import React from 'react';

interface ButtonProps {
  children: React.ReactNode;
  onClick?: () => void;
  variant?: 'primary' | 'secondary';
}

export const Button: React.FC<ButtonProps> = ({ 
  children, 
  onClick, 
  variant = 'primary' 
}) => {
  const styles = variant === 'primary' 
    ? 'bg-black text-white' 
    : 'bg-gray-200 text-black';
  
  return (
    <button 
      className={`px-4 py-2 rounded ${styles}`}
      onClick={onClick}
    >
      {children}
    </button>
  );
};
```

**2. Экспортируй в `src/components/index.ts`:**
```typescript
export { Badge } from './Badge';
export { Button } from './Button';  // ← ДОБАВЬ ЭТО
```

**3. Используй в App.tsx:**
```typescript
import { Button } from './src/components';

<Button variant="primary" onClick={() => alert('Hi!')}>
  Click me
</Button>
```

**Готово! 🎉**

---

## 🎓 Третья задача: Создать hook

### Задача: Создать hook для переключения (toggle)

**1. Создай файл `src/hooks/useToggle.ts`:**
```typescript
import { useState } from 'react';

export const useToggle = (initialValue: boolean = false) => {
  const [isOpen, setIsOpen] = useState(initialValue);
  
  const toggle = () => setIsOpen(!isOpen);
  const open = () => setIsOpen(true);
  const close = () => setIsOpen(false);
  
  return { isOpen, toggle, open, close };
};
```

**2. Экспортируй в `src/hooks/index.ts`:**
```typescript
export { useToggle } from './useToggle';  // ← ДОБАВЬ
export { useFolders } from './useFolders';
```

**3. Используй в компоненте:**
```typescript
import { useToggle } from './src/hooks';

export const MyComponent = () => {
  const { isOpen, toggle } = useToggle(false);
  
  return (
    <div>
      <button onClick={toggle}>
        {isOpen ? 'Close' : 'Open'}
      </button>
      {isOpen && <p>Menu is open!</p>}
    </div>
  );
};
```

**Готово! 🎉**

---

## 📚 Все документы

| Документ | Для кого | Когда читать |
|---|---|---|
| README.md | Все | Сейчас! |
| QUICK_REFERENCE.md | Разработчики | Нужна справка |
| DEVELOPER_GUIDE.md | Разработчики | Полное изучение |
| PROJECT_STRUCTURE.md | Архитекторы | Понимание структуры |
| REFACTORING_SUMMARY.md | Менеджеры | Что было сделано |
| TODO.md | Планировщики | План развития |

---

## ❓ Что если...

### Я не понимаю где находится функция?
→ Прочитай [QUICK_REFERENCE.md](./QUICK_REFERENCE.md)

### Я не знаю как импортировать?
→ Прочитай [DEVELOPER_GUIDE.md](./DEVELOPER_GUIDE.md#импорт-примеры)

### Мне нужно добавить новый компонент?
→ Прочитай [DEVELOPER_GUIDE.md](./DEVELOPER_GUIDE.md#примеры-использования)

### Приложение не запускается?
1. Проверь что установлены зависимости: `npm install`
2. Проверь что Node.js версия 18+: `node -v`
3. Посмотри ошибку в терминале
4. Прочитай [README.md](./README.md#troubleshooting)

---

## 🤝 Есть вопросы?

1. **Про структуру?** → PROJECT_STRUCTURE.md
2. **Про функции?** → QUICK_REFERENCE.md
3. **Про написание кода?** → DEVELOPER_GUIDE.md
4. **Про ошибку?** → Посмотри консоль браузера

---

## ✅ Чек-лист новичка

- [ ] Я прочитал README.md
- [ ] Я понимаю структуру папок
- [ ] Я знаю где находятся утилиты
- [ ] Я знаю как использовать hooks
- [ ] Я запустил приложение и оно работает
- [ ] Я добавил свою первую функцию
- [ ] Я создал свой первый компонент
- [ ] Я готов к разработке! 🚀

---

## 🎉 Результат

Когда завершишь все шаги:
- ✅ Ты поймёшь структуру проекта
- ✅ Ты сможешь быстро ориентироваться в коде
- ✅ Ты сможешь добавлять новые функции
- ✅ Ты будешь продуктивным разработчиком

---

**Удачи! Тебе будет интересно разрабатывать этот проект! 🚀✨**

P.S. Если что-то непонятно, прочитай [DEVELOPER_GUIDE.md](./DEVELOPER_GUIDE.md) - там всё подробно объяснено!
