
# 🚀 Rafoz Matrix Manager — Очень простая инструкция

**Rafoz Matrix Manager** — это приложение для работы с таблицами, фильтрами и данными.

**Новое:** Проект был рефакторен на модульную архитектуру! 📚 [Подробнее в REFACTORING_REPORT.md](./REFACTORING_REPORT.md)

---

## 📁 Структура проекта

```
src/
├── components/        # UI компоненты
├── hooks/            # Custom React hooks
├── utils/            # Утилиты и функции
├── types.ts          # TypeScript интерфейсы
├── constants.ts      # Константы приложения
└── App.tsx           # Главный компонент
```

Подробнее → [PROJECT_STRUCTURE.md](./PROJECT_STRUCTURE.md)  
Руководство для разработчиков → [DEVELOPER_GUIDE.md](./DEVELOPER_GUIDE.md)

---

## 🛠 1. Что нужно заранее

Нужно всего 2 вещи:
* **VS Code** (редактор).
* **Node.js 18+** (скачай с [nodejs.org](https://nodejs.org/)).

---

## 📦 2. Установка

1. Открой папку проекта в VS Code.
2. Открой терминал (View → Terminal).
3. Напиши:
    ```bash
    npm install
    ```

---

## 🚀 3. Запуск

1. В терминале напиши:
    ```bash
    npm run dev
    ```
2. Открой браузер и перейди на:
    ```
    http://localhost:3000
    ```
Готово. Приложение работает.

---

## 🐳 3.1 Запуск через Docker (stage по умолчанию)

**Нужно:** Docker Desktop.

**Stage (по умолчанию):**
```bash
docker compose up --build
```

**Dev (hot reload):**
```bash
docker compose --profile dev up --build
```

**Prod (финальная статика):**
```bash
docker compose --profile prod up --build
```

Во всех режимах приложение доступно на:
```
http://localhost:3000
```

---

## 💎 4. Простыми словами (что важно)

Приложение работает по строгим правилам, которые мы внедрили:

1. **Есть лимиты:**
    * Названия — до 50 символов.
    * Описания и комментарии — до 500 символов.
    * Когда лимит достигнут — дальше ввод не пускает.

2. **Длинный текст обрезается** в таблице, полный текст виден по клику на строку.

3. **Подсказки:** наведи на название — увидишь полный текст.

4. **Pairwise режим:** если вводить через `/`, создаются комбинации.

5. **Авто‑ID:** при создании таблицы можно включить ID. Значения ID менять нельзя.

---

## 📂 5. Где что лежит

* `src/App.tsx` — всё главное.
* `src/types.ts` — типы данных.
* `src/constants.ts` — лимиты и константы.
* `src/index.tsx` — старт приложения.
* `index.html` — HTML оболочка.

---

## 🛠 6. Если что-то не работает

* **Стили пропали:** перезапусти `npm run dev`.
* **Ошибки в терминале:** удали `node_modules` и снова `npm install`.
* **Белый экран:** открой DevTools (F12) и посмотри ошибку в Console.

---

**Разработано с акцентом на UX/UI и производительность.** 
*Enjoy your data management.* 🔥
